package estree

import (
	"testing"
)

func TestJSX1(t *testing.T) {
	// 	ast, err := compile("<a:b.c />")
	// 	assert.Equal(t, nil, err, "should be prog ok")

	// 	assert.EqualJson(t, `

	// `, ast)
}
